#include <stdio.h>

int main()
{
    puts("hi my name is Suchir"); // puts appears on each new line, automatically adds a /n
    puts("What is your name?");

    printf("Hi I go to NCSU/n I am a Sophomore"); // printf prints on a single line, will only print on multiple lines if a /n is specified

    return (0);
}
